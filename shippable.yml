language: python
reset_minion: true

python:
  - 2.7
  # - 3.2
  # - 3.3
  # - 3.4
  # - pypy
  
install:
  - pip install scipy
  - "python -c 'import scipy'"
  - pip install -r requirements.txt

script:
  - nosetests geometry 
  