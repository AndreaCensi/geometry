language: python
reset_minion: true

python:
  - 2.7
  # - 3.2
  # - 3.3
  # - 3.4
  # - pypy
  
install:
  - sudo apt-get install python-numpy
  - sudo apt-get install python-scipy
  - pip install -r requirements.txt

script:
  - nosetests geometry 
  